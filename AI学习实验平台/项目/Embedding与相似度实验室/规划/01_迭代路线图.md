# 迭代路线图

## V0.1 当前版本

- 建立独立项目目录。
- 完成前端实验台。
- 完成 FastAPI 后端。
- 接入统一平台项目切换。
- 编写学习文档。

## V0.2 真实 embedding 模型

可选方向：

- 接入 Qwen embedding API。
- 接入 OpenAI embedding API。
- 接入 BGE 本地 embedding。
- 对比教学语义维度 embedding 和真实 embedding。

## V0.3 和文本切分联动

下一项目“文本切分实验室”完成后，可以让切片结果直接送入本项目计算 embedding。

## V0.4 和向量库联动

向量库实验室完成后，把 embedding 向量写入 Chroma/FAISS/pgvector。

## V0.5 和 Rerank 联动

Rerank 实验室完成后，把 Top K 初召回结果交给 rerank 模型重新排序。
